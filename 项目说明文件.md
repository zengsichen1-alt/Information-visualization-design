# 月球文化信息可视化 — 项目说明文档

## 项目简介

本项目是一个以 **月球文化** 为主题的 3D 信息可视化单页 Web 应用。通过 Three.js 构建 3D 月球场景，结合 GSAP 滚动驱动动画，将世界各文明的月亮神话传说与月球探测历史数据进行视觉化呈现。

用户可通过滚动浏览深入探索：月球模型的自转与陨石场景、各文明的月亮神话（中国、希腊、印度、日本、罗马、北欧、西欧、埃及），以及历史上月球探测器的实际着陆点分布。

---

## 技术栈

| 类别        | 技术                                               |
| ----------- | -------------------------------------------------- |
| 3D 渲染引擎 | Three.js (WebGL, 多场景多视口渲染)                 |
| 动画引擎    | GSAP (ScrollTrigger, SplitText)                    |
| 平滑滚动    | Lenis                                              |
| 3D 模型格式 | GLTF/GLB                                           |
| CSS         | 原生 CSS3 (CSS 自定义属性, CSS Anchor Positioning) |
| 音频        | HTML5 Audio API                                    |
| 地图数据    | HDR 环境贴图, 经纬度坐标系统                       |

### 依赖库文件

| 文件                                    | 说明                  |
| --------------------------------------- | --------------------- |
| `js/gsap.min.js`                        | GSAP 核心动画库       |
| `js/ScrollTrigger.min.js`               | GSAP 滚动触发器插件   |
| `js/SplitText.min.js`                   | GSAP 文字分割动画插件 |
| `js/DrawSVGPlugin.min.js`               | GSAP SVG 绘制插件     |
| `threejs/Three.js`                      | Three.js 主库         |
| `threejs/jsm/loaders/GLTFLoader.js`     | GLTF/GLB 模型加载器   |
| `threejs/jsm/loaders/HDRLoader.js`      | HDR 环境贴图加载器    |
| `threejs/jsm/controls/OrbitControls.js` | 轨道控制器            |

---

## 文件结构

```
信息可视化/
├── index.html                    # 主页面（~1622行）
├── 项目说明文件.md               # 本文档
├── bak.txt                       # 历史备份代码
├── css/
│   └── main.css                  # 全局样式表（~849行）
├── js/
│   ├── main.js                   # GSAP 动画逻辑 & UI 交互（~441行）
│   ├── scene.js                  # Three.js 场景管理（~371行）
│   ├── mouseEvent.js             # 鼠标跟随相机控制器类（~50行）
│   ├── gsap.min.js               # GSAP 核心库
│   ├── ScrollTrigger.min.js      # 滚动触发器
│   ├── SplitText.min.js          # 文字分割
│   └── DrawSVGPlugin.min.js      # SVG 绘制
├── threejs/                      # Three.js 引擎及附属模块
│   ├── Three.js                  # 主入口
│   ├── Three.Core.js             # 核心
│   ├── jsm/
│   │   ├── loaders/              # 加载器
│   │   └── controls/             # 控制器
│   └── ...                       # 其他模块
├── model/                        # 3D 模型文件
│   ├── moon4.glb                 # 月球模型（用于陨石场景 & 探索场景）
│   ├── earthLow.glb              # 低精度地球模型
│   ├── earthMap.glb              # 地球贴图模型
│   └── cloudseaL.glb             # 云层模型
├── pic/                          # 图片资源
│   ├── HDR_subdued_blue_nebulae.hdr  # 背景 HDR 天空球贴图
│   ├── 长箭头-copy-copy.svg      # 箭头图标
│   ├── 长箭头W.svg               # 白色箭头图标
│   ├── header-menu-dots.svg      # 菜单圆点图标
│   ├── sound-control-btn-pic-black.svg  # 音频按钮图标
│   ├── Wikipedia.svg             # 维基百科图标
│   ├── china/                    # 中国文化区域图片
│   ├── GREECE/                   # 希腊文化区域图片
│   ├── in/                       # 印度文化区域图片
│   ├── JP/                       # 日本文化区域图片
│   ├── NE/                       # 北欧文化区域图片
│   ├── ROME/                     # 罗马文化区域图片
│   └── satellite/                # 探测器图片资源
└── audio video/
    ├── Evolution_of_the_Moon.ogv.240p.vp9.webm  # 月球演化视频
    └── silent.mp3                # 背景音频
```

---

## 核心功能模块

### 1. Loading 加载动画（`main.js` #12-#27）

页面加载完成后显示 Loading 界面，包含：

- 渐隐消失动画（opacity 淡出）
- 自定义进度条动画（CSS `@keyframes loadBar`）
- 文字呼吸效果（CSS `@keyframes pulse`）

### 2. 多场景 Multi-Scene 渲染（`scene.js`）

项目使用 Three.js 的 **Scissor Test** 技术，在单个 Canvas 上渲染多个独立场景到不同 DOM 区域：

| 场景                 | 容器选择器                 | 用途                                  |
| -------------------- | -------------------------- | ------------------------------------- |
| `backgroundScene`    | `.canvas-container`        | 全屏背景 HDR 天空球（带鼠标跟随微动） |
| `moonmeteoriteScene` | `.moonmeteorite-container` | 月球陨石场景（月球绕轴自转）          |
| `moonExploreScene`   | `.moonExplore-container`   | 月球探索场景（滚动驱动旋转+标记点）   |

渲染循环 `animate()` 按帧遍历场景列表，逐容器更新视口并渲染。

### 3. 鼠标跟随相机控制器（`mouseEvent.js`）

`MouseFollowCameraController` 类：

- 监听全局 `mousemove` 事件
- 根据鼠标在屏幕上的归一化位置，平滑插值相机偏航/俯仰角
- 支持自定义最大偏移角 (`maxYaw`/`maxPitch`) 和插值速度 (`lerpSpeed`)
- `dispose()` 方法清理事件监听，防止内存泄漏

### 4. 滚动驱动动画系统（`main.js`）

基于 GSAP ScrollTrigger 的滚动驱动动画体系：

- **Section2 标题动画**：文字逐字 Y 轴位移入场 + X 轴水平偏移（错帧延迟）
- **Section2 介绍文字**：Y轴位移 + 透明度渐变
- **Section3 标题动画**：文字 Y 轴位移 + 旋转入场
- **Section3 文化卡片**：`clipPath` 裁剪动画（inset 由 10% 到 0%），文字 `SplitText` 逐字符旋转 360° 入场
- **Section4 探索页**：月球模型旋转同步滚动进度（`scrub: 1`），标记点大小同步滚动触发器

### 5. UI 交互系统（`main.js`）

| 组件                 | 功能                     | 动画详情                                                                 |
| -------------------- | ------------------------ | ------------------------------------------------------------------------ |
| **音频控制按钮**     | 控制背景音乐播放/暂停    | 按钮缩放呼吸动画 (bounce yoyo)                                           |
| **菜单按钮**         | 展开/折叠导航菜单        | 两点图标 90° 旋转 + 菜单面板斜向入场                                     |
| **菜单项悬停**       | 箭头 + 文字向上位移      | 箭头缩放入场 + 文字/克隆文字 Y 轴位移                                    |
| **维基百科菜单**     | 右侧特殊菜单项           | 45° 箭头位移 + 文字向上 (带克隆层)                                       |
| **返回按钮**         | 关闭文化详情页           | 箭头水平位移 (本体位移 + 克隆从右侧入) + 文字位移，按钮自身渐变显示/隐藏 |
| **文化区域卡片**     | 点击展开对应文化详情     | 过场黑屏 + 详情页淡入，背景滚动禁用                                      |
| **文化详情横向滚动** | 鼠标滚轮驱动内容横向平移 | `wheel` 事件捕获，GSAP `x` 位移，限幅钳制                                |

### 6. 月球探索系统（`scene.js` #113-#278）

- 通过 **经纬度坐标** 计算 12 个月球探测器着陆点在 3D 球面上的准确位置
- 滚动联动模型旋转：`rotateModelOnScroll()` 函数使月球模型随滚动进度旋转，将对应任务区域转向用户
- 标记点缩放动画：`addMarker()` 创建红色球体标记，通过 ScrollTrigger `scrub` 同步显示/隐藏
- 旋转吸附 (`snap`)：滚动停止后自动吸附到目标旋转角度

#### 着陆点经纬度数据

| 任务       | 纬度    | 经度    | 说明                                  |
| ---------- | ------- | ------- | ------------------------------------- |
| Luna 2     | 29.1°N  | 0.0°    | 苏联月球2号（首个撞击月球的人造物体） |
| Ranger 7   | 10.35°S | 20.58°W | 美国徘徊者7号                         |
| Luna 9     | 7.13°N  | 64.37°W | 苏联月球9号（首个软着陆）             |
| Surveyor 1 | 2.45°S  | 43.22°W | 美国勘测者1号                         |
| Apollo 11  | 0.67°N  | 23.47°E | 阿波罗11号（首次载人登月）            |
| Apollo 12  | 3.01°S  | 23.42°W | 阿波罗12号                            |
| Apollo 14  | 3.65°S  | 17.47°W | 阿波罗14号                            |
| Apollo 15  | 26.13°N | 3.63°E  | 阿波罗15号                            |
| Apollo 16  | 8.97°N  | 15.5°E  | 阿波罗16号                            |
| Apollo 17  | 20.19°N | 30.77°E | 阿波罗17号                            |
| Chang'E 3  | 44.12°N | 19.51°E | 中国嫦娥三号                          |
| Chang'E 5  | 43.1°N  | 51.8°W  | 中国嫦娥五号                          |

---

## 各文化区域内容概览

项目在 Section3 中以卡片网格形式展示 8 个文明的月亮神话传说，点击卡片进入详情页（横向滚动）：

| 区域                  | 文化/文明                                | 位置坐标           |
| --------------------- | ---------------------------------------- | ------------------ |
| 中国 (China)          | 嫦娥奔月、玉兔、吴刚伐桂                 | `pic/china/`       |
| 希腊 (Greece)         | 塞勒涅 (Selene)、阿尔忒弥斯 (Artemis)    | `pic/GREECE/`      |
| 印度 (India)          | 苏摩 (Soma)、月神旃陀罗                  | `pic/in/`          |
| 日本 (Japan)          | 辉夜姬 (Kaguya-hime)、月读命 (Tsukuyomi) | `pic/JP/`          |
| 罗马 (Rome)           | 露娜 (Luna)、狄安娜 (Diana)              | `pic/ROME/`        |
| 北欧 (Norse)          | 玛尼 (Máni)                              | `pic/NE/`          |
| 西欧 (Western Europe) | —                                        | （图片资源待确认） |
| 埃及 (Egypt)          | 托特 (Thoth)、孔苏 (Khonsu)              | —                  |

每个文化区域卡片包含：

- 文化图片（`region-pic`）
- 地理位置文字（`location`）
- 悬停效果：箭头 + 大字标位移入场
- 点击展开详情页（`region-content`）：包含故事标题、故事内容、图片展示

---

## 页面结构

```
Page (滚动容器)
├── Section1 — 首屏
│   ├── 背景 HDR 天空球 (canvas-container)
│   ├── 月球陨石场景 (moonmeteorite-container)
│   └── 标题文字 (title)
├── Section2 — 世界地图
│   ├── 标题动画文字 (title-map)
│   ├── 介绍文字 (title-map-introduce)
│   └── 月球演化视频 (worldmap-container)
├── Section3 — 文化区域
│   ├── 标题 (title-nation)
│   ├── 文化卡片网格 (nation-culture)
│   └── 区域详情页 (region-content ×8)
└── Section4 — 月球探索
    ├── 3D 月球探索场景 (moonExplore-container)
    └── 多页探测器介绍 (explore-page)
```

---

## CSS 架构要点（`main.css`）

- **CSS 自定义属性**：`--base-padding-y`, `--base-padding-x` 用于响应式间距
- **CSS Anchor Positioning**：`anchor-name: --header-right` 实现菜单相对于按钮的定位
- **隐藏滚动条**：`body::-webkit-scrollbar { display: none; }`
- **过场动画层**：`.turning-screen` 绝对定位居中，用于文化详情切换黑屏过渡
- **星空背景**：（从 bak.txt 迁移的 `body::before`/`::after` 伪元素星空动画，当前未启用）
- **响应式单位**：大量使用 `vw` 视口单位，适配不同屏幕尺寸

---

## 使用方式

### 环境要求

- 现代浏览器（支持 WebGL、CSS Anchor Positioning）
- 启动本地服务器（因为需加载 GLB/HDR 等外部资源，file:// 协议可能受限）

### 本地运行

```bash
# 使用 Python 启动简易服务器
cd 信息可视化
python -m http.server 8080

# 或使用 Node.js (需安装 http-server)
npx http-server -p 8080
```

然后在浏览器打开 `http://localhost:8080`。

### 交互操作

| 操作          | 效果                         |
| ------------- | ---------------------------- |
| 滚轮向下      | 驱动页面滚动，触发各区域动画 |
| 鼠标移动      | 3D 场景相机微动跟随          |
| 点击音频按钮  | 背景音乐播放/暂停            |
| 点击菜单按钮  | 展开导航菜单                 |
| 悬停菜单项    | 箭头 & 文字动画              |
| 点击文化卡片  | 进入文化详情页               |
| 详情页内滚轮  | 横向滚动文化内容             |
| 点击返回按钮  | 退出详情页，回到主界面       |
| 滚动 Section4 | 月球模型旋转，标记点缩放     |

---

## 设计亮点

1. **多场景共 Canvas 渲染**：通过 Scissor Test 节省 WebGL 上下文，在单个画布渲染 3 个独立 3D 场景
2. **滚动与 3D 模型联动**：GSAP ScrollTrigger 的 `scrub` 模式实现滚动驱动月球旋转与标记点动画
3. **经纬度球面定位**：将地理坐标精准映射到 3D 球面，实现探测器标记的准确放置
4. **CSS Anchor Positioning**：利用现代 CSS 特性实现菜单相对于触发按钮的动态定位
5. **平滑滚动体验**：Lenis 提供惯性平滑滚动，配合 GSAP 动画实现沉浸式浏览
6. **文化横向滚动**：详情页使用自定义 wheel 事件处理器实现横向平移，突破传统纵向布局

---

## 潜在改进方向

- 启用 `bak.txt` 中的星空粒子背景 (`body::before`/`::after`)
- 完善 Apollo 11~17 和嫦娥 3/5 标记点的 ScrollTrigger 配置（代码中已注释）
- 添加 `earthMap.glb` 的世界地图 3D 场景（代码中已注释）
- 添加响应式适配（移动端优化）
- 文化详情页加载更多文本内容
- 接入多语言支持

---

_最后更新：2026年5月_
